{
  "name": "glyde-functions",
  "description": "Server-side push delivery for Glyde (alerts, announcements, schedule reminders) — sends via FCM even when the app is closed.",
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^16.8.0"
  },
  "private": true
}
